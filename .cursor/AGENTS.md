# go-stream – Contexto para o agente

Repositório contém **joinlegends-backend** (Go/Fiber) e possivelmente frontend.

Para o **backend** (JoinLegends), use como referência principal:

**`joinlegends-backend/docs/CONTEXT_FOR_LLM.md`**

Esse documento descreve arquitetura, estrutura de pastas, padrões (Controller → Service → Repository), convenções de repositórios e models, e como adicionar novos domínios. Ao trabalhar em código Go do backend, consulte-o para manter consistência.
